[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# REST requests
requests = "*"
# for scrapping html websites
beautifulsoup4 = "*"
requests-html = "*"
# for .env file loading
python-dotenv = "*"
# for data type check
pydantic = "*"
sqlmodel = "*"
# orm
sqlalchemy = {version = "*", extras = ["asyncio,mypy"]}
# async postgres driver
asyncpg = "*"
databases = {extras = ["postgresql"], version = "*"}
# http framework
fastapi = "*"
# async gunicorn alternative
uvicorn = "*"
#code auto-formatting tool
pre-commit = "==2.19.0"
#logging
sentry-sdk = "==1.6.0"
# for Dependency injection and start o IoC
injector = "*"
# patches asyncio so we can start nested loop in the main loop
# this is beacause we can pass the couroutine in this web serverver
# env - or research would be needed
nest-asyncio = "==1.5.6"

[dev-packages]

[requires]
python_version = "3.8"
